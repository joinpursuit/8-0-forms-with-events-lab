console.log("Code your solution!")
document.forms.submit

function addTodo(button){
    let addThis = document.todoForm.display.value
    addThis = "put an LI tag here."
}
//this function, once one item is added, a list must be created
//add unordered list

function createList{
    add <UL>

}